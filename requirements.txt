# =========================
# Dépendances communes
# =========================
numpy==1.26.4
opencv-python==4.8.1.78
matplotlib==3.7.5
scikit-learn==1.3.2
scipy==1.10.1
pillow>=9.0

# =========================
# TensorFlow / Keras
# =========================

# macOS Apple Silicon (M1 / M2 / M3)
tensorflow-macos==2.10.0 ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal==0.6.0  ; platform_system == "Darwin" and platform_machine == "arm64"
keras==2.10.0            ; platform_system == "Darwin" and platform_machine == "arm64"

# macOS Intel
tensorflow==2.10.1 ; platform_system == "Darwin" and platform_machine == "x86_64"
keras==2.10.0      ; platform_system == "Darwin" and platform_machine == "x86_64"

# Linux (x86_64)
tensorflow==2.10.1 ; platform_system == "Linux" and platform_machine == "x86_64"
keras==2.10.0      ; platform_system == "Linux" and platform_machine == "x86_64"

# Windows (x86_64)
tensorflow==2.10.1 ; platform_system == "Windows"
keras==2.10.0      ; platform_system == "Windows"

